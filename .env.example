# Postgres connection string. For local dev you can run Postgres in Docker,
# or use a free hosted instance (Neon, Supabase, Railway) for both dev & prod.
DATABASE_URL="postgresql://user:password@localhost:5432/forumapp"

# Generate with: openssl rand -base64 32
AUTH_SECRET="replace-with-a-long-random-string"
